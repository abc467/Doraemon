[Unit]
Wants=doraemon-runtime.service
PartOf=doraemon-runtime.service

[Service]
TimeoutStartSec=180
ExecStartPre=/bin/bash -lc 'for i in $(seq 1 120); do timeout 1 bash -lc "</dev/tcp/127.0.0.1/9090" && exit 0; sleep 1; done; echo "rosbridge 127.0.0.1:9090 not ready" >&2; exit 1'
